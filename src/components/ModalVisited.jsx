import styles from "./ModalVisited.module.css";

export default function ModalVisited() {
	return (
		<div>
			<main>
				<p>Have u already visited this spot?</p>

				<div>
					<button>Yes</button>
					<button>No</button>
				</div>
			</main>
		</div>
	);
}
