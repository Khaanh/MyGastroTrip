import "./style.css";

const cities = [
	"München",
	"Augsburg",
	"Lindau",
	"Würgzburg",
	"Stuttgart",
	"Frankfurt",
];

export default function ListOfCity(cities) {
	return (
		<ul>
			{/* {cities.map((city) => (
				<li>{city}</li>
			))} */}
		</ul>
	);
}
